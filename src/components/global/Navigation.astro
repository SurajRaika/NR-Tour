<div   data-aos="zoom-in" class="">
  <div class="px-8 md:px-12 mx-auto max-w-7xl py-2 lg:px-32 text-xs">
    <div
      x-data="{ open: false }"
      class="relative flex flex-col w-full mx-auto rounded-md md:rounded-lg md:items-center md:justify-between md:flex-row"
    >
      <div class="flex flex-row items-center justify-between md:justify-start">
        <a class="text-black font-bold hover:text-zinc-500 items-center font-ld uppercase gap-3 inline-flex font-display text-pretty" href="/">
          Rajasthan Taxi Services
        </a>
        <button
          @click="open = !open"
          class="inline-flex items-center justify-center p-2 text-black hover:text-blue-500 focus:outline-none focus:text-black md:hidden"
        >
          <svg class="w-6 h-6" stroke="currentColor" fill="none" viewBox="0 0 24 24">
            <path :class="{'hidden': open, 'inline-flex': !open }" class="inline-flex" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            <path :class="{'hidden': !open, 'inline-flex': open }" class="hidden" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>
      <nav :class="{'flex': open, 'hidden': !open}" class="flex-col flex-grow hidden py-12 md:py-0 md:flex md:items-end justify-center md:flex-row">
        <ul class="space-y-2 list-none md:space-y-0 md:ml-auto items-center md:inline-flex justify-center text-center md:text-left gap-3">
          <li><a href="/services/one-way" class="text-black   font-bold hover:text-blue-500 font-display text-pretty">One-Way</a></li>
          <li><a href="/TaxiPackages" class="text-black       font-bold hover:text-blue-500 font-display text-pretty">Taxi Packages</a></li>
          <li>
            <a href="tel:+917976592083" class="py-2 px-6 border focus:ring-2 rounded-[2px]  border-transparent bg-black hover:bg-black/90 text-white duration-200 focus:ring-offset-2 focus:ring-white font-bold inline-flex items-center justify-center ring-1 ring-transparent lg:ml-auto">
              Call Now
            </a>
          


          </li>
        </ul>
      </nav>
    </div>
  </div>
</div>





<section>
  <div class="px-8 md:px-12 mx-auto max-w-7xl py-12 lg:px-32">
    <ol role="list" class="flex flex-wrap gap-3">
      {["Airport", "One-Way", "Local", "Outstation", "Tour"].map((tag) => (
        <li class="inline-flex items-center uppercase mx-auto hover:text-blue-500 text-xs font-medium text-black">
          <a href={`/services/${tag.toLowerCase()}`}>{tag}</a>
        </li>
      ))}
    </ol>
  </div>
</section>

<style>
  .m-modal-content {
    background: #1e1e1e;
    color: #ffffff;
    padding: 20px;
    border-radius: 2px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    max-width: 400px;
    margin: auto;
    font-family: 'Inter', sans-serif;
}

.m-title {
    font-size: 1.5rem;
    font-weight: bold;
    color: #f8f8f8;
    display: block;
    margin-bottom: 10px;
    text-align: center;
}

.m-details {
    font-size: 0.9rem;
    color: #bdbdbd;
    text-align: center;
    margin-bottom: 15px;
}

.m-open {
    display: block;
    background: #2e2e2e;
    color: #ffffff;
    padding: 10px;
    margin: 8px 0;
    text-align: center;
    border-radius: 8px;
    text-decoration: none;
    transition: background 0.3s;
}

.m-open:hover {
    background: #3a3a3a;
}

.m-copy {
    display: block;
    background: #444;
    color: #ffffff;
    padding: 10px;
    text-align: center;
    border-radius: 8px;
    text-decoration: none;
    cursor: pointer;
    transition: background 0.3s;
}

.m-copy:hover {
    background: #555;
}

.m-by {
    display: block;
    text-align: center;
    font-size: 0.8rem;
    color: #888;
    margin-top: 10px;
    text-decoration: none;
}

.m-by:hover {
    color: #aaa;
}

</style>
